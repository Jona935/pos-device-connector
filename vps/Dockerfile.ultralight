# Archivos para VPS (Ubuntu)
FROM python:3.11-slim

WORKDIR /app

# Instalar solo Flask y requests (ultraligero)
RUN pip install flask requests --no-cache-dir

# Copiar solo la API del servidor
COPY app_ultralight.py ./

EXPOSE 5000

CMD ["python", "-u", "app_ultralight.py"]